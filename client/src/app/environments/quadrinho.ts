import { Base } from "./baseUrl"
export const quadrinho = {
    getQuadrinho: Base.url + 'getQuadrinho',
    getQuadrinhos: Base.url + 'getQuadrinhos',
    createQuadrinho: Base.url + 'createQuadrinho',
    deleteQuadrinho: Base.url + 'deleteQuadrinho',
}
